pluginManagement {
    // Include plugin module for composite build (allows development without publishing)
    includeBuild('plugin')

    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // For MPAndroidChart
        maven { url = uri("https://jitpack.io") }
    }
}

rootProject.name = 'chart-a11y'

// Note: ':plugin' is NOT included here because it's used as a composite build
// via includeBuild('plugin') in pluginManagement block above
include ':runtime'
include ':sample'
